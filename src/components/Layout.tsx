import styled from "styled-components";

const Layout = styled.main`
  display: grid;
  grid-template-columns: 1fr 385px;
  gap: 2rem;
  margin-right: 0 auto;
  padding: 4rem 1rem;
`;

export { Layout };
